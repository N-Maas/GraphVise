# here we store external libraries that we build ourselves
# and that we add as libraries in our main parent directory

# Dear Imgui is a lightweight gui library
add_subdirectory(imgui)

# Glad provides OpenGL extensions
add_subdirectory(glad)
